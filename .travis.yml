language: python
before_install:
        -python --version
os:
        - linux
jobs:          
        include:
                - stage: test
                  script:  
                        - cd testing && python run_random.py ../vampire
  
